services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: {{APP_NAME}}-app
    ports:
      - "{{PORT}}:8080"
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
